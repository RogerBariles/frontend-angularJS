'use strict';

describe('myApp.data module', function() {

    beforeEach(module('myApp.data'));

    describe('data controller', function() {

        it('should ....', inject(function($controller) {
            //spec body
            var view2Ctrl = $controller('dataController');
            expect(view2Ctrl).toBeDefined();
        }));

    });
});